package net.franzka.sgilt.core.prestataire.domain;

/**
 * Clés d'engagement d'un prestataire. Source de vérité côté back.
 * Le rendu (icône, label, description, couleur) est la responsabilité du front.
 */
public enum Engagement {
    REPONSE_48H,
    ADAPTABLE,
    ACCOMPAGNEMENT,
    EQUIPE,
    INTERLOCUTEUR_UNIQUE,
    ECORESPONSABLE
}
