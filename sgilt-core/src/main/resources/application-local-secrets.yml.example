sgilt:
  jwt:
    # Générer avec : openssl rand -base64 64
    confirmation-secret: <secret>
  keycloak:
    # Keycloak admin → Clients → sgilt-admin → Credentials → Client secret
    admin-client-secret: <secret>
