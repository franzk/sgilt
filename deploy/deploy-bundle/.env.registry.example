# ===================================
# MODE: REGISTRY (Pull images from ghcr.io)
# ===================================
# This file configures the deployment using the registry-based workflow.
# Images are pulled from GitHub Container Registry (ghcr.io).

# ===================================
# IMAGE VERSIONS (ghcr.io)
# ===================================

# Définir les images à utiliser depuis le registry
KA_FRONT_VERSION=1.0.0
KA_GATEWAY_VERSION=1.0.0
SERVICE_EXAMPLE_VERSION=1.0.0
KA_SMTP_BRIDGE_VERSION=1.0.0
KA_MAILER_VERSION=1.0.0
KA_KEYCLOAK_VERSION=1.0.0

# ===================================
# APPLICATION ENVIRONMENT VARIABLES
# ===================================

# Project
PROJECT=ka-starter

# Keycloak
KEYCLOAK_URL=https://auth.example.com
KEYCLOAK_ISSUER=https://auth.example.com/realms/ka-starter
KEYCLOAK_HOST=ka-keycloak:8080

# Frontend
VITE_KEYCLOAK_REALM=ka-starter
VITE_KEYCLOAK_CLIENT_ID=ka-front
VITE_API_URL=https://api.example.com

# Gateway
BACK_URL=http://service-example:8080
APP_URL=https://app.example.com

# Database
KC_DB_PASSWORD=change-me

# Admin
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=change-me

# SMTP
MAILER_URL=http://ka-mailer:8080
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=user@example.com
SMTP_PASSWORD=change-me
SMTP_AUTH=true
SMTP_SSL=true

# Proxy (optionnel)
# PROXY_NETWORK_NAME=traefik-public

