/**
 * 'preview' : rendu identique à 'display' (lecture seule), sauf le chrome de réservation
 * (datepicker + bouton de réservation), masqué — utilisé par l'onglet Preview de l'éditeur.
 */
export type DisplayMode = 'display' | 'edit' | 'preview' | 'IA'
