meta {
  name: onboarding - 3. set password
  type: http
  seq: 6
}

post {
  url: {{apiUrl}}/api/v1/onboarding/confirm-account
  body: json
  auth: none
}

body:json {
  {
    "setPasswordToken": "<SET_PASSWORD_TOKEN_FROM_STEP_2>",
    "password": "Motdepasse123!"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
